///get_image_speed_from_frames(frames, image_number)
/*
    Returns the image speed required for one loop of
    frames in an animation.
*/
var frames = argument0;
var number = argument1;

return (1/(frames/number));
